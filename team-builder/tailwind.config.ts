const config = {
  theme: {
    extend: {
      colors: {
        softBg: "#F8FAFC",
      },
      boxShadow: {
        soft: "0 10px 30px -5px rgba(0, 0, 0, 0.04), 0 4px 15px -5px rgba(0, 0, 0, 0.02)",
        "inner-soft": "inset 0 2px 4px 0 rgba(0, 0, 0, 0.02)",
      },
      borderRadius: {
        soft: "2rem",
      },
    },
  },
};
